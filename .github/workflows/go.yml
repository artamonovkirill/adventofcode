name: Go

on:
  push:
    path-ignore:
      - 2021/
      - .github/workflows/gradle.yml

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-go@v4
        with:
          go-version: 1.19.1
      - run: make test
